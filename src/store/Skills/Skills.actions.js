import { createActions } from 'redux-actions';

/** @typedef {object} SkillsActions
 * @property { function } somePlaceholder
 *  */

const skillsActions = createActions(
    'SOME_PLACEHOLDER',
)

export default skillsActions