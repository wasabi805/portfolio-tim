import { createActions } from 'redux-actions';

/** @typedef {object} ExperienceActions
 * @property { function } somePlaceholder
 *  */

const experienceActions = createActions(
    'SOME_PLACEHOLDER',
)

export default experienceActions;