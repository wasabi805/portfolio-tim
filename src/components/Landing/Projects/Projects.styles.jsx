import { css } from 'emotion';

export const projectsWrapper = css({
    position: 'relative',
    display: 'block',

    "& h2":{
        textAlign: 'center'
    },
    "& a ":{
        textDecoration : 'none'
    }
});