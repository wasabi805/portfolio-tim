import { css } from 'emotion';

export const homeSection = css({
    width: '100vw',
    padding: '0',
    margin: '0',
});

export const heroBackgroundImg = css({
    width: '100%',
    opacity: '0.4',
})
